# stargazers-log
A log of the repositories I've starred.

## Starred Repositories Page
Open `index.html` in a browser to view the starred repositories page.

This page loads sample repository data from `events.json`, applies layout styles from `style.css`, and renders the list using `script.js`.
